version: "2"
sql:
  - engine: "sqlite"
    schema: "schema/schema.sql"
    queries: "queries"
    gen:
      go:
        package: "sql"
        out: "gen"
        emit_interface: true
        omit_unused_structs: false
